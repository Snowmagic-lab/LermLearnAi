# LermLearn — คู่มือผู้ทดสอบ

## สิ่งที่ผู้ทดสอบต้องมี

1. Windows 10/11 64-bit และอินเทอร์เน็ต
2. บัญชี Google ที่เข้า Google Classroom ของตนเองได้
3. API key ของ AI หนึ่งคีย์ — แนะนำ Qwen

## ขั้นตอนติดตั้ง

1. เปิด `LermLearn Setup 0.1.0.exe` แล้วติดตั้ง
2. เปิด LermLearn จาก Desktop หรือ Start Menu
3. เลือกผู้ให้บริการ AI และวาง API key หนึ่งคีย์ในหน้าตั้งค่าเริ่มต้น
4. กดบันทึกและเปิดโปรแกรม
5. กด **เชื่อมต่อ Google Classroom** เลือกบัญชี Google ของตนเอง และกดยอมรับสิทธิ์ที่ต้องการ
6. ตรวจว่ารายวิชาและงานที่แสดงเป็นข้อมูลของบัญชีตนเอง

ผู้ทดสอบ **ไม่ต้องกรอก Google OAuth Client ID, Client Secret และไม่ต้องเพิ่มอีเมลใน Test users** ในเวอร์ชันติดตั้งนี้ OAuth Client ID ของแอปถูกเตรียมไว้ให้แล้ว ผู้ใช้แต่ละคนเพียงล็อกอินและอนุญาตบัญชีของตนเอง

หาก Google แสดงคำเตือนว่าแอปยังไม่ผ่านการยืนยัน ให้ตรวจชื่อแอปและรายการสิทธิ์ก่อนดำเนินการต่อ หากบัญชีโรงเรียนบล็อกแอปภายนอก ให้ติดต่อผู้ดูแลระบบ

## คีย์ AI

- [Qwen API key — แนะนำ](https://modelstudio.console.alibabacloud.com/?tab=dashboard#/api-key)
- [Gemini API key](https://aistudio.google.com/app/apikey)
- [DeepSeek API key](https://platform.deepseek.com/api_keys)

ผู้ทดสอบควรใช้ API key ของตนเอง ไม่ควรใช้คีย์ร่วมกันหลายคน เพราะอาจทำให้โควตาเต็ม คีย์จะเก็บในเครื่องของผู้ทดสอบและไม่ควรส่งผ่านแชตหรือภาพหน้าจอ

## สิ่งที่ควรทดสอบ

- เชื่อมต่อและออกจากระบบ Google
- ห้องเรียน รายวิชา งาน และกำหนดส่ง
- ปฏิทินการเรียนและแผนการทำงานภายในแอป
- เลือกไฟล์ PDF, PowerPoint, Word หรือรูปภาพ แล้วกดสรุป
- สร้างข้อสอบแบบเลือกตอบและตรวจคะแนน/เฉลย/เหตุผล
- สร้างแฟลชการ์ดและเปิดดูคำตอบทีละใบ
- ดาวน์โหลด Summary, Quiz หรือ Flashcard เป็น PDF
- เปิดโหมดออฟไลน์เมื่อไม่มี API หรืออินเทอร์เน็ต

หากระบบอ่านไฟล์ไม่ได้ ต้องแสดงว่าอ่านไม่ได้และไม่แต่งเนื้อหาแทน กรุณาบันทึกชื่อไฟล์ ขั้นตอนที่ทำ และภาพหน้าจอที่ไม่เปิดเผย API key เพื่อส่งให้ผู้พัฒนา

## ข้อมูลและสิทธิ์ Google

LermLearn สามารถอ่านตารางจาก Google Calendar เพื่อใช้ประกอบการวางแผนการเรียนและการทำงาน โดยคำนึงถึงเวลาว่าง เวลาพัก และเวลานอนของผู้ใช้

ในเวอร์ชันทดลอง ระบบยังไม่แก้ไขข้อมูลใน Google Calendar โดยตรง เพื่อจำกัดสิทธิ์การเข้าถึงข้อมูลส่วนตัวของผู้ใช้และเพิ่มความปลอดภัย ปฏิทินการทำงานของ LermLearn สามารถจัดและแก้ไขแผนได้ภายในแอป

Calendar ใช้เพื่ออ่านข้อมูลประกอบการวางแผน ส่วนแผนที่ผู้ใช้แก้ไขใน LermLearn เป็นข้อมูลภายในแอป ไม่ใช่กิจกรรมที่ถูกเขียนกลับไปยัง Google Calendar

## ทดสอบจาก source code

```powershell
npm install
Copy-Item .env.example .env.local
npm run dev
```

หรือเปิด desktop mode ด้วย:

```powershell
npm run desktop
```

ใส่ API key ใน `.env.local` ของเครื่อง Dev เท่านั้น ห้าม commit `.env.local` และ `google-token.json`

สร้างตัวติดตั้งใหม่ด้วย:

```powershell
npm run dist
```

ผลลัพธ์อยู่ที่ `dist\LermLearn Setup 0.1.0.exe`

# คู่มือผู้ทดสอบ LermLearn

## เส้นทางทดสอบสำหรับกรรมการ

ดาวน์โหลดไฟล์ `LermLearn Setup 0.1.0.exe` จากหน้า GitHub Release/Tag เท่านั้น ไม่ต้องดาวน์โหลด source code หรือไฟล์จากโฟลเดอร์ `dist-logo-fix`

ตัวติดตั้งรุ่นนี้ไม่ฝัง AI API key และไม่ฝัง Google OAuth Client Secret ผู้ทดสอบจึงต้องใส่ AI API key ของตนเองครั้งแรก แต่ไม่ต้องกรอก Client ID, Client Secret หรือ Redirect URI

## สิ่งที่ผู้ทดสอบต้องทำ

1. ติดตั้งจากไฟล์ `LermLearn Setup 0.1.0.exe`
2. เปิดโปรแกรม
3. เลือกผู้ให้บริการ AI และใส่ API key ของตนเองในหน้าตั้งค่าครั้งแรก
4. กดบันทึกและเปิดโปรแกรม
5. กด **เชื่อมต่อ Google Classroom**
6. เลือกบัญชี Google ตรวจสอบสิทธิ์ และกดยอมรับ

ผู้ทดสอบไม่ต้องกรอก Google OAuth Client ID, Client Secret และไม่ต้องเพิ่มอีเมลใน Test users เพราะ Client ID ของแอปถูกเตรียมไว้ในโปรแกรมแล้ว

## API key ที่แนะนำ

แนะนำ Qwen สำหรับสรุปเอกสาร สร้างข้อสอบ และแฟลชการ์ด:

- Qwen: https://bailian.console.alibabacloud.com/?tab=api#/api-key
- Gemini: https://aistudio.google.com/app/apikey
- DeepSeek: https://platform.deepseek.com/api_keys

API key จะถูกเก็บไว้เฉพาะเครื่องของผู้ทดสอบ ห้ามส่งคีย์ให้ทีมงานหรืออัปโหลดลง GitHub

## การทดสอบที่แนะนำ

- ตรวจว่ารายวิชาและงานจาก Google Classroom แสดงตามบัญชีที่ล็อกอิน
- ตรวจงานที่ยังไม่เสร็จ งานที่ส่งแล้ว และกำหนดส่ง
- เปิดห้องติว เลือกไฟล์ PDF, PowerPoint, Word หรือไฟล์ข้อความไม่เกิน 50 MB
- ทดสอบ Summary, Quiz และ Flashcard แยกกัน
- ตรวจเหตุผลและแหล่งอ้างอิงของผลลัพธ์
- ทดสอบปฏิทินและการจัดแผนโดยไม่ให้เวลานอนหรือเวลาพักถูกใช้เกินความเหมาะสม
- ทดสอบโหมดออฟไลน์เมื่อไม่มี API key หรืออินเทอร์เน็ต

## ข้อจำกัดที่ควรแจ้งผู้ทดสอบ

- หากไฟล์อ่านไม่ได้ ระบบต้องแจ้งว่าอ่านไม่ได้และไม่ควรแต่งเนื้อหาแทน
- หาก AI ตอบช้าหรือ API quota เต็ม ให้ลองใหม่ภายหลังหรือใช้โหมดออฟไลน์
- Google อาจแสดงคำเตือน “แอปยังไม่ผ่านการยืนยัน” ผู้ใช้ควรตรวจสอบสิทธิ์ก่อนกดยอมรับ
- บัญชีโรงเรียนอาจถูกนโยบายของผู้ดูแลระบบบล็อก OAuth
- LermLearn อ่าน Google Calendar เพื่อประกอบการวางแผน แต่ในเวอร์ชันทดลองยังไม่แก้ไขข้อมูลใน Google Calendar โดยตรง แผนงานสามารถจัดและแก้ไขได้ภายในแอป
